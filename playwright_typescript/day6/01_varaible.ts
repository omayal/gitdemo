let companyname :string ="testleaf"
console.log(companyname);
/**
 * data type in ts => any , never , tuple , array(homogenous)
 * 
 */
let firstname="omayal" //implicit
let lastname :string="kattiresan"//explicit

/**
 * type inference  =>can infer datatype on its own
 * 1.implicit =>When ts is able to infer datatype on its own from the value that you are assigning
 * 2.explicit  =>when the user explicitly mention the datatype 
 */